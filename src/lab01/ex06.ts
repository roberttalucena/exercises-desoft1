/* 

Write the function isEven(n: number): boolean to compute if a given number is even (or odd).

*/

function isEven(n: number): boolean {
  if (n % 2 == 0) {
    return true;
  } 
  else {
    return false;
  }
}

export{isEven};
  